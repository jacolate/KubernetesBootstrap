apiVersion: kubeadm.k8s.io/v1beta3
kind: ClusterConfiguration
kubernetesVersion: "v1.30.1"  # Replace with your desired Kubernetes version
controlPlaneEndpoint: "{{ apiserver_endpoint }}:6443"  # Replace with your load balancer's address
apiServer:
  certSANs:
  - {{ api2 }}  # Additional IP
networking:
  serviceSubnet: "10.96.0.0/12"  # Replace with your desired service subnet
  podSubnet: {{ cluster_cidr }}
